interface MainCompProps {
}

export default function MainComponent({ }: MainCompProps) {


    return (<div className="relative flex-1 grid grid-cols-2 sm:grid-cols-4 lg:grid-cols-5 gap-1">
        Vite + React + TS + Tailwind – Hello World
    </div>)
}